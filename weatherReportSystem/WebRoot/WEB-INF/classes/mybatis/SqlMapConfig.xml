<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- 定义别名 -->
	<typeAliases>
		<!-- 批量定义别名
		    指定包名，mybatis自动扫描包中的po类，自定义别名，别名就是类名（首字母大小写都可以） -->
		 <package name="cn.wrs.po"/>
	</typeAliases>
</configuration>